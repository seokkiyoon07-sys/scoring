# 자동 채점 시스템 개발 가이드

> 개발자 및 AI를 위한 프로젝트 상세 문서

## 📑 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [핵심 개념](#핵심-개념)
3. [파일 구조](#파일-구조)
4. [데이터 형식](#데이터-형식)
5. [채점 로직](#채점-로직)
6. [코드 구조](#코드-구조)
7. [개발 환경](#개발-환경)
8. [주의사항](#주의사항)
9. [트러블슈팅](#트러블슈팅)
10. [향후 개선사항](#향후-개선사항)

---

## 프로젝트 개요

### 목적
OMR 또는 수기 답안을 CSV 형식으로 변환한 후, 과목코드별로 자동 채점하는 시스템

### 기술 스택
- **언어**: Python 3.13+
- **프레임워크**: Streamlit (웹 GUI)
- **주요 라이브러리**: pandas (데이터 처리)
- **환경**: macOS (Apple Silicon 호환)

### 주요 기능
- CSV 파일 업로드 (드래그 앤 드롭)
- 과목코드별 자동 채점
- 점수, 정답수, 오답번호 계산
- 통계 분석 (평균, 최고점, 최저점)
- 결과 CSV 다운로드

---

## 핵심 개념

### ⚠️ 가장 중요한 규칙: 과목코드별 채점

```
과목코드 1: 1번~45번 문항 (독립적인 정답 세트)
과목코드 2: 1번~45번 문항 (독립적인 정답 세트)
과목코드 3: 1번~45번 문항 (독립적인 정답 세트)
...
```

**각 과목코드는 완전히 독립적인 문항 세트를 가집니다!**

### 채점 흐름
1. 정답 파일을 읽어서 **과목코드별로 분리** (딕셔너리 생성)
2. 학생 답안을 한 줄씩 읽음
3. 해당 학생의 **과목코드를 확인**
4. 그 과목코드의 정답으로만 채점
5. 결과 저장

### 예시
```python
# 정답 데이터 구조
answer_dict = {
    '과목1': {
        'answers': [1, 3, 2, 4, 1, ...],  # 45개
        'points': [10, 10, 10, 10, 10, ...]  # 45개
    },
    '과목2': {
        'answers': [2, 4, 1, 3, 2, ...],  # 45개 (과목1과 다름!)
        'points': [10, 10, 10, 10, 10, ...]
    }
}

# 학생A (과목1) → answer_dict['과목1']로 채점
# 학생B (과목2) → answer_dict['과목2']로 채점
```

---

## 파일 구조

```
scoring/
├── app.py                      # 메인 애플리케이션
├── requirements.txt            # 패키지 의존성
├── README.md                   # 사용자 매뉴얼
├── DEVELOPMENT_GUIDE.md        # 이 문서 (개발 가이드)
├── .gitignore                  # Git 제외 파일
├── .streamlit/
│   └── config.toml            # Streamlit 설정
├── venv/                       # 가상환경 (자동 생성)
├── sample_students.csv         # 테스트용 학생 답안
└── sample_answers.csv          # 테스트용 정답/배점
```

---

## 데이터 형식

### 1. 학생 답안 파일 (CSV)

#### 구조
| 열 번호 | 열 이름 | 설명 | 예시 |
|--------|---------|------|------|
| columns[0] | 수험번호 | 학생 식별자 | 2024001 |
| columns[1] | **과목코드** | 과목 식별자 (매우 중요!) | MATH01, ENG01 |
| columns[2:] | 1번, 2번, 3번, ... | 학생의 각 문항 답안 | 1, 3, 2, 4, ... |

#### 예시 파일
```csv
수험번호,과목코드,1번,2번,3번,4번,5번,...,45번
2024001,과목1,1,3,2,4,1,2,3,4,1,2,...
2024002,과목1,1,2,2,4,1,2,4,4,1,2,...
2024003,과목2,2,3,1,4,2,1,3,2,4,1,...
2024004,과목2,2,4,1,4,2,1,3,2,4,2,...
```

#### 중요 규칙
- **첫 번째 줄은 헤더** (열 이름)
- **두 번째 열 (columns[1])이 과목코드** ⭐️
- 세 번째 열부터가 실제 답안
- 답안은 문자열 또는 숫자 가능
- 빈칸(NA)은 오답 처리

### 2. 정답 및 배점 파일 (CSV)

#### 구조
| 열 번호 | 열 이름 | 설명 | 예시 |
|--------|---------|------|------|
| columns[0] | 과목번호 | 과목 식별자 | MATH01, ENG01 |
| columns[1] | 문항 | 문항 번호 (1~45) | 1, 2, 3, ... |
| columns[2] | 정답 | 해당 문항의 정답 | 1, 2, 3, 4, 5 |
| columns[3] | 배점 | 해당 문항의 점수 | 10, 5, 20 |

#### 예시 파일
```csv
과목번호,문항,정답,배점
과목1,1,1,10
과목1,2,3,10
과목1,3,2,10
...
과목1,45,2,10
과목2,1,2,10
과목2,2,4,10
과목2,3,1,10
...
과목2,45,1,10
```

#### 중요 규칙
- **과목번호는 학생 답안의 과목코드와 정확히 일치**해야 함
- 문항 번호는 1부터 시작 (순서대로 정렬됨)
- 배점은 숫자여야 함 (정수 또는 실수)
- 한 과목에 대해 모든 문항이 있어야 함

### 3. 결과 파일 (CSV)

#### 구조
```csv
수험번호,과목코드,총점,만점,정답수,오답번호
2024001,과목1,430,450,43/45,"3, 15"
2024002,과목1,450,450,45/45,없음
2024003,과목2,420,450,42/45,"5, 12, 30"
```

---

## 채점 로직

### 전체 흐름도

```
[정답 파일 로드]
      ↓
[과목코드별로 분리 및 딕셔너리 생성]
      ↓
[학생 답안 파일 로드]
      ↓
FOR 각 학생:
    ├─ 학생의 과목코드 읽기
    ├─ 해당 과목의 정답/배점 가져오기
    ├─ 문항별 채점 수행
    ├─ 총점, 정답수, 오답번호 계산
    └─ 결과 저장
      ↓
[결과 데이터프레임 반환]
```

### 상세 채점 알고리즘

```python
# 1단계: 정답 데이터 준비
answer_dict = {}
for 과목 in 정답파일.unique_subjects():
    과목별_정답 = 정답파일[과목코드 == 과목]
    과목별_정답 = 과목별_정답.sort_by(문항번호)  # 순서 정렬
    answer_dict[과목] = {
        'answers': 과목별_정답[정답열].tolist(),
        'points': 과목별_정답[배점열].tolist()
    }

# 2단계: 학생별 채점
for 학생 in 학생들:
    수험번호 = 학생[columns[0]]
    과목코드 = 학생[columns[1]]  # ⭐️ 두 번째 열
    학생답안 = 학생[columns[2:]]  # 세 번째 열부터
    
    # 해당 과목의 정답 가져오기
    정답들 = answer_dict[과목코드]['answers']
    배점들 = answer_dict[과목코드]['points']
    
    총점 = 0
    정답개수 = 0
    오답목록 = []
    
    # 문항별 채점
    for i in range(len(정답들)):
        if i < len(학생답안):
            학생의_답 = 학생답안[i]
            정답 = 정답들[i]
            배점 = 배점들[i]
            
            if 학생의_답 == 정답:
                총점 += 배점
                정답개수 += 1
            else:
                오답목록.append(i + 1)
        else:
            # 학생이 답을 안 적은 경우
            오답목록.append(i + 1)
    
    결과저장(수험번호, 과목코드, 총점, 만점, 정답개수, 오답목록)
```

### 답안 비교 로직

```python
# 숫자와 문자 모두 지원
try:
    # 숫자로 변환 시도 (1, 1.0, "1" 모두 동일 처리)
    if float(학생답안) == float(정답):
        정답처리()
except:
    # 문자열 비교 (공백 제거)
    if str(학생답안).strip() == str(정답).strip():
        정답처리()
```

---

## 코드 구조

### 주요 함수

#### 1. `load_student_data(file)`
**목적**: 학생 답안 CSV 파일 로드

```python
def load_student_data(file):
    """
    학생 답안 파일을 읽어서 pandas DataFrame으로 반환
    UTF-8, CP949 인코딩 자동 감지
    """
    try:
        df = pd.read_csv(file, encoding='utf-8')
    except:
        df = pd.read_csv(file, encoding='cp949')
    return df
```

#### 2. `load_answer_data(file)`
**목적**: 정답/배점 CSV 파일 로드

```python
def load_answer_data(file):
    """
    정답/배점 파일을 읽어서 pandas DataFrame으로 반환
    UTF-8, CP949 인코딩 자동 감지
    """
    try:
        df = pd.read_csv(file, encoding='utf-8')
    except:
        df = pd.read_csv(file, encoding='cp949')
    return df
```

#### 3. `grade_students(student_df, answer_df)` ⭐️ 핵심
**목적**: 실제 채점 수행

**입력**:
- `student_df`: 학생 답안 DataFrame
- `answer_df`: 정답/배점 DataFrame

**출력**:
- 채점 결과 DataFrame (수험번호, 과목코드, 총점, 만점, 정답수, 오답번호)

**내부 로직**:
1. 학생 파일과 정답 파일의 열 구조 확인 및 디버깅 출력
2. 정답 데이터를 과목코드별로 딕셔너리로 변환
3. 각 학생별로 반복:
   - 과목코드 확인
   - 해당 과목의 정답으로 채점
   - 총점, 정답수, 오답번호 계산
4. 결과 DataFrame 반환

**주의사항**:
- `student_df.columns[1]`이 과목코드
- `answer_df.columns[0]`이 과목번호
- 이 둘이 정확히 매칭되어야 함

### 데이터 흐름

```
Streamlit UI
    ↓
[파일 업로드]
    ↓
load_student_data() → student_df
load_answer_data() → answer_df
    ↓
grade_students(student_df, answer_df)
    ↓
result_df
    ↓
[화면 표시 + CSV 다운로드]
```

---

## 개발 환경

### 설치

```bash
# 1. 프로젝트 디렉토리로 이동
cd /Volumes/SeokkiMAC/Coding/Myproject/OCR/scoring

# 2. 가상환경 생성
python3 -m venv venv

# 3. 가상환경 활성화
source venv/bin/activate

# 4. 패키지 설치
pip install -r requirements.txt
```

### 실행

```bash
# 가상환경 활성화 (매번 필요)
source venv/bin/activate

# Streamlit 앱 실행
streamlit run app.py

# 브라우저 자동 실행 또는 수동 접속
# http://localhost:8501
```

### 종료

```bash
# Streamlit 종료
Ctrl + C

# 또는
pkill -f streamlit

# 가상환경 비활성화
deactivate
```

---

## 주의사항

### ⚠️ 반드시 지켜야 할 사항

1. **과목코드 매칭**
   - 학생 답안의 과목코드와 정답 파일의 과목번호가 정확히 일치해야 함
   - 대소문자, 공백 모두 구분됨

2. **열 순서**
   - 학생 답안: `수험번호`, `과목코드`, `1번`, `2번`, ...
   - 정답 파일: `과목번호`, `문항`, `정답`, `배점`
   - 순서가 바뀌면 안 됨!

3. **문항 수 일치**
   - 정답 파일의 각 과목별 문항 수와 학생 답안의 문항 수가 일치하지 않아도 됨
   - 정답 파일 기준으로 채점 (부족한 문항은 오답 처리)

4. **인코딩**
   - UTF-8 또는 CP949(EUC-KR) 지원
   - 한글이 깨지면 엑셀에서 "CSV UTF-8" 형식으로 저장

5. **배점 타입**
   - 배점은 반드시 숫자여야 함
   - 문자열로 읽히는 경우 float()로 변환됨

### 🐛 알려진 이슈

1. **Streamlit 경고 메시지**
   ```
   Please replace `use_container_width` with `width`.
   ```
   - 기능에는 영향 없음
   - 향후 Streamlit 업데이트 시 수정 예정

2. **첫 실행 시 이메일 입력 요청**
   - `.streamlit/config.toml`로 해결됨
   - 이미 설정되어 있으면 문제없음

---

## 트러블슈팅

### 문제: 점수가 0으로 나옴

**원인**:
- 과목코드 불일치
- 데이터 타입 불일치
- 열 순서 잘못됨

**해결**:
1. 디버깅 메시지 확인
2. 과목코드가 정확히 일치하는지 확인
3. 답안이 숫자/문자열 형식 확인

### 문제: 과목코드 인식 안 됨

**원인**:
- 공백이나 특수문자 포함
- 대소문자 차이

**해결**:
```python
# 엑셀에서 데이터 정리
=TRIM(A2)  # 공백 제거
```

### 문제: 한글 깨짐

**원인**:
- 인코딩 문제

**해결**:
1. 엑셀: 다른 이름으로 저장 → "CSV UTF-8"
2. 또는 메모장에서 UTF-8로 저장

### 문제: 서버 실행 안 됨

**원인**:
- 포트 충돌
- 패키지 미설치

**해결**:
```bash
# 기존 프로세스 종료
pkill -f streamlit

# 패키지 재설치
pip install -r requirements.txt

# 다시 실행
streamlit run app.py
```

---

## 향후 개선사항

### 우선순위 높음

1. **디버깅 모드 토글**
   - 현재는 항상 디버깅 정보 표시
   - 프로덕션에서는 숨기기 옵션 필요

2. **에러 처리 강화**
   - 파일 형식 오류 시 상세한 안내 메시지
   - 필수 열 누락 시 명확한 에러

3. **대용량 파일 처리**
   - 현재는 전체 파일을 메모리에 로드
   - 청크 단위 처리로 개선 가능

### 우선순위 중간

4. **통계 기능 확장**
   - 과목별 평균
   - 문항별 정답률
   - 성적 분포 그래프

5. **다중 파일 업로드**
   - 여러 시험지를 한 번에 채점
   - 배치 처리

6. **엑셀 파일 지원**
   - .xlsx 직접 읽기
   - CSV 변환 없이 사용

### 우선순위 낮음

7. **사용자 계정 관리**
   - 로그인 기능
   - 채점 기록 저장

8. **API 제공**
   - REST API로 외부 시스템 연동
   - 자동화 가능

---

## 코드 수정 가이드

### 열 순서 변경 시

```python
# app.py 58행 근처
# 수험번호가 3번째 열이라면
id_col = student_df.columns[2]  # 0 → 2로 변경

# 과목코드가 1번째 열이라면
subject_col = student_df.columns[0]  # 1 → 0으로 변경
```

### 새로운 통계 추가 시

```python
# app.py 200행 근처 (통계 섹션)
with col5:
    # 새로운 통계 추가
    median_score = result_df['총점'].median()
    st.metric("중앙값", f"{median_score}점")
```

### UI 변경 시

```python
# Streamlit 컴포넌트 참고
st.title()      # 제목
st.subheader()  # 부제목
st.write()      # 텍스트
st.dataframe()  # 표
st.metric()     # 지표
st.button()     # 버튼
st.info()       # 정보 박스
st.warning()    # 경고 박스
st.error()      # 에러 박스
st.success()    # 성공 박스
```

---

## 테스트

### 단위 테스트 (권장)

```python
# test_grading.py (향후 추가 예정)
import pytest
from app import grade_students

def test_grade_single_student():
    """단일 학생 채점 테스트"""
    # 테스트 데이터 준비
    student_df = pd.DataFrame({
        '수험번호': ['TEST001'],
        '과목코드': ['MATH01'],
        '1번': [1],
        '2번': [3],
        '3번': [2]
    })
    
    answer_df = pd.DataFrame({
        '과목번호': ['MATH01', 'MATH01', 'MATH01'],
        '문항': [1, 2, 3],
        '정답': [1, 3, 2],
        '배점': [10, 10, 10]
    })
    
    # 채점 실행
    result = grade_students(student_df, answer_df)
    
    # 검증
    assert result['총점'][0] == 30
    assert result['만점'][0] == 30
    assert result['정답수'][0] == '3/3'
```

### 통합 테스트

1. `sample_students.csv`와 `sample_answers.csv`로 테스트
2. 예상 결과:
   - 2024001: 100점 (10/10)
   - 2024002: 80점 (8/10)
   - ...

---

## 기여 가이드

### 코드 스타일

- Python PEP 8 준수
- 함수에 docstring 작성
- 의미 있는 변수명 사용
- 한글 주석 허용

### 커밋 메시지

```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅
refactor: 코드 리팩토링
test: 테스트 추가
chore: 빌드/설정 변경
```

---

## 문의

프로젝트 관련 문의나 버그 제보는 이슈로 등록해주세요.

---

**Last Updated**: 2025-10-15
**Version**: 1.0.0
**Author**: OCR Scoring Team


